@model CONTRAST_WEB.Models.TravelRequestHelper
@{
    ViewBag.Title = "Home Page";
    Layout = "~/Views/Shared/_LayoutHome.cshtml";
}

@using (Html.BeginForm("Index", "Menu", FormMethod.Post))
{
    <div class="container">
        <div class="row">
            <div style="margin:200px auto" class="Absolute-Center">
                <div class="col-sm-12 col-md-10 col-md-offset-1">
                    <hr />
                        <form class="width" id="loginForm">
                            <div2 class="form-group input-group">
                                <!--<input class="form-control" type="text" name='username' placeholder="username" />-->
                                @Html.TextBoxFor(m => m.employee_info.code, new { @class = "form-control", placeholder = "No Reg" })
                                <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                            </div2>
                            <div class="form-group input-group">
                                <!--<input class="form-control" type="password" name='password' placeholder="password" />-->
                                <!--<span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>-->
                            </div>
                            <div class="form-group">
                                <!--<p class="btn btn-info btn-block">@*@Html.ActionLink("Home", "Menu", new { id = item.id_request })*@</p>-->
                                <input type="submit" value="Login" /> 
                                <!--<a href="/Home/Menu"><button type="button" class="btn btn-info btn-block"><span class="glyphicon glyphicon-log-in"></span>&nbsp;Sign In</button></a>-->
                            </div>
                        </form>
                    <footer class="footer modal-footer">
                        <p style="text-align:center">&copy; @DateTime.Now.Year - PT Toyota Astra Motor</p>
                    </footer>
                    </div>
            </div>
          </div>
      </div>
}